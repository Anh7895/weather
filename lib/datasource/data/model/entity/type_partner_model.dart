import 'package:flutter/cupertino.dart';

class TypePartnerModel {
  String? key;
  String? name;
  Icon? imageAssets;
  String? note;
  bool? isSelected;

  TypePartnerModel(
      {this.key, this.name, this.imageAssets, this.note, this.isSelected});

}
